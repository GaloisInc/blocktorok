-- INCOMPLETE: CANNOT BE COMPLETED UNTIL OPENFOAM SYNTAX IS DECIDED

config {
  timeDomain: transient; -- steady state or transient simulation
  timeStep : 0.005 s;-- size of the time step
  totalTime : 1 s; -- total number of time steps
  const Temp = 273.0 K; -- SOLID_TEMPERATURE_INIT
  run: m1(Temp);
  backend : Su2 {
      format : LIB_InputOutput
  };
}

model m1(qw) {
  technique : FVM;
  -- openfoam expects the mesh to be in constant/polyMesh folder or generated by blockMeshDict
  mesh: blockMeshDict;
  boundaryField : LIB_BoundaryCondition;
  physics : HeatConduction{LIB_SolvingTechnique};
  const D_T = 0.000127 m^2/x;
  dT/dt - D_T*△T = 0
  solve Tw with {LIB_NumericalScheme, LIB_PhysicsParameters};
  return Tw;
}