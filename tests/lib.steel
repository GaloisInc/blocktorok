-- imagine a library

Model Settings


-- General (can be in any model)


-- Only applies in Fluid models
Fluid.COMPRESSIBLE_FREESTREAM_DEFINITION

Fluid.Sutherland --   ViscosityModel

Fluid.THERMALCONDUCTIVITYMODEL

Fluid.standardAir
-- default settings:
--   ratio_Specific_Heat = 1.4
--   GasConstant =  287.058 J/kg*K


-- Only applies in Structure models

Structure.CONDUCTION_CONDITION_DEFINITION
-- default settings:
--   Solid Density= 8.5 g/cm3 (chromel)
--   Solid Specific Heat = ? J/kg*K (depends on the temperature)
--   Solid Thermal Conductivity = ? W/m*K (depends on the temperature)
